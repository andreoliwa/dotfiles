#!/usr/bin/env bash
# https://github.com/riobard/bash-powerline
# Loaded only on remote servers (RPi + Hetzner).
# shellcheck source=/dev/null
[ -f "$HOME/.bash-powerline.sh" ] && source "$HOME/.bash-powerline.sh"
