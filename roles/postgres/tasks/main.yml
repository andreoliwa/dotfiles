- name: "Start the PostgreSQL Docker container"
  command: "docker-compose -f {{ role_path }}/docker-compose.yml up -d"

- name: "Backup all local PostgreSQL databases to .sql files in {{ backup_dir }}"
  command: "pg-tool postgresql://postgres:postgres@localhost:5440 backup {{ backup_dir }}"
