# Lazily load rvm, irb, rake, rails

export PATH="$PATH:$HOME/.rvm/bin:~/.gem/ruby/2.6.0/bin" # Add RVM to PATH for scripting

# This only works with zsh, on the original dotfiles repo
# group_lazy_load $HOME/.rvm/scripts/rvm rvm irb rake rails
